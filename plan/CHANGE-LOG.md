# Round-2 Change Log

> One row per client change, in the order the client gave them. IDs are permanent — never renumber.
> "Tabs touched" links must match a `R2-NN` entry inside each named tab file.
> Statuses: 🟢 planned · 🟡 needs input (see Q ref) · 🔵 discuss later · ⚪ superseded.

| ID | Client ask (short, their words) | Interpretation (one line) | Tabs touched | Schema touched | Overlaps / depends | Status |
|---|---|---|---|---|---|---|
| R2-01 | "Logins with different access — username + password; roles owner / employee / accountant; owner adds employees in settings; access discussed later" | Replace PIN gate with real multi-user auth (username+password on Supabase Auth), 3 roles, owner-managed users in Settings; per-role access matrix deferred | [login-shell](tab-login-shell.md), [settings](tab-settings.md) (+ every tab once Q-01 matrix lands) | `smark_app_users` (new), RLS baseline rewrite, actor/created_by now real user ids | Access matrix → **Q-01** · PIN fate → **Q-02** · "accountant" role hints at future cost/finance features (watch for a later change) | 🟡 |
| R2-02 | "Track employee attendance, along with what project they are working on" | New Attendance area: employees check in/out daily + current-project tag; owner sees team view | [attendance](tab-attendance.md) (new), [login-shell](tab-login-shell.md) (nav) | `smark_attendance` (new) | Capture mechanics + who-sees-what → **Q-03**; employees = R2-01 users; project link = R2-03 | 🟡 |
| R2-03 | "Order tab becomes a Projects tab; each project has multiple different BOM uploads, each named differently" | Rename Orders → Projects; project = hub; BOMs become a named list per project; sourcing pipeline (setup/run/review) runs per-BOM | [orders-projects](tab-orders-projects.md), [login-shell](tab-login-shell.md), [ordering-workspace](tab-ordering-workspace.md), [agent-run](tab-agent-run.md), [order-review](tab-order-review.md), [on-order](tab-on-order.md), [bulk-pick](tab-bulk-pick.md) | `smark_boms.name` unique per project; per-BOM saved runs; project status derived | Baseline was 1 BOM per project — supersedes that assumption everywhere | 🟢 |
| R2-04 | "Each project has employees assigned; track how many hours they have worked on it and stuff" | Project team: assign employees to a project + log hours per employee per project | [orders-projects](tab-orders-projects.md), [attendance](tab-attendance.md) | `smark_project_members`, `smark_time_entries` (new) | Hours capture mechanics (manual vs from attendance) → **Q-03**; needs R2-01 users, R2-03 project hub | 🟡 |
| R2-05 | "Estimated timeline for the project, shared with their clients as input — to manage and do this stuff we will discuss these later" | Per-project estimated timeline; some client-facing sharing/input mechanism — mechanics explicitly parked by client | [orders-projects](tab-orders-projects.md) | `smark_projects` timeline columns (minimal now) | Sharing/input mechanics → **Q-04** (client said discuss later) | 🔵 |
| R2-06 | "Per project: inputs like meeting notes, changes, tasks and stuff — owner or employee can add and track/keep" | Project activity log: typed entries (note / meeting / change / task), tasks with done-state, owner+employee can write | [orders-projects](tab-orders-projects.md) | `smark_project_activities` (new) | Lives in R2-03 project hub; visibility for accountant → Q-01 | 🟢 |
| R2-07 | "New daily reports tab: attendance marked by themself, working hours, what they work on, inventory movements today (who took/added what), ordering activity — everything done by everyone today gets tracked" | New Daily Reports tab = per-day digest (attendance + hours + movements + ordering events); absorbs the standalone Attendance tab (R2-02 surfaces move in) | [daily-reports](tab-daily-reports.md) (new), [attendance](tab-attendance.md) (merged), [login-shell](tab-login-shell.md) | day-digest view over existing tables (no new table); confirms attendance = self-marked | Partially answers **Q-03(a)**; hours model still Q-03(b) | 🟡 |
| R2-08 | "We will not mark as ordered in the review tab; review should be stored after a run so client can visit later; reviews tab only adds to cart" | Review = persisted per run (selections included), revisitable; Mark-ordered removed; single action = Add to cart | [order-review](tab-order-review.md), [agent-run](tab-agent-run.md) | selection persisted on `smark_agent_results`; ordering columns move to cart | Feeds R2-09 cart | 🟢 |
| R2-09 | "Rename on-order tab as cart; here we add quantity and order the item required quantity; also ask them to add price" | On-order → **Cart**: cart lines (from review + auto-shortfall + manual), editable order qty, price entered at ordering; Ordered/Arrived sections stay below | [on-order→cart](tab-on-order.md), [order-review](tab-order-review.md) | `smark_cart_items` (new) | Checkout mechanics → R2-12; shortfall lines → R2-10 | 🟢 |
| R2-10 | "Smart cart: if item in inventory is demanded by multiple projects (500 avail, A needs 400, B needs 200) auto-add the extra 100 to cart" | Cross-project demand aggregation vs available stock; shortfall auto-added as cart line with per-project breakdown | [on-order→cart](tab-on-order.md), [orders-projects](tab-orders-projects.md) | demand/shortfall view over active BOM lines | Demand lifecycle (register/release) → **Q-05** | 🟡 |
| R2-11 | "Track the price of each item and show the inventory value in the dashboard" | Per-part unit price (last paid; history already in part events); dashboard gains Inventory value ₹ stat | [dashboard](tab-dashboard.md), [part-detail](tab-part-detail.md), [inventory](tab-inventory.md) | `smark_parts.last_unit_price` (+currency) | Prices fed by R2-09 manual entry + R2-12 receipt extraction | 🟢 |
| R2-12 | "Order is not per project — order items for all projects at once; option to upload order details/receipt and extract price (saved); per order a required PO number" | Checkout = ONE global purchase across projects (lines keep project/BOM traceability); `PO number` required per order; receipt upload → R2 + AI price extraction fills line prices | [on-order→cart](tab-on-order.md), [receive](tab-receive.md) | `smark_orders`: global scope, `po_number` required, receipt fields | Changes invariant "orders live inside a project" → now per-line traceability | 🟢 |
| R2-13 | "Metadata per item more detailed: dates, price, employee name, quantity, when/what, which client, when ordered, everything with timestamps, old price/new price, where ordered from" | Enrich the part living record: every event timestamped with actor, qty, price, PO/distributor, project→client; new `price_change` events (old→new); fuller event coverage | [part-detail](tab-part-detail.md) | `smark_part_events` enrichment (+price_old/new, order ref, more event types) | Builds on R2-11/12 price flow; client shown via project join | 🟢 |
| R2-14 | "Project management should track its performance, completion expected, stuff like that" | Project overview gains progress/performance: completion %, expected vs actual completion, hours vs estimate | [orders-projects](tab-orders-projects.md) | `smark_projects` +completion cols (draft) | What exactly defines "performance/completion" → **Q-07** | 🟡 |
| R2-15 | "Also maybe payments and stuff (per project)" | Per-project payments — client said *maybe*; parked; schema seam = income entries with project link | [orders-projects](tab-orders-projects.md), [expenses](tab-expenses.md) | via `smark_expenses.project_id` seam | Folded into finance-fields discussion → **Q-09** | 🔵 |
| R2-16 | "Obviously document uploads to store everything with its name (per project)" | Project Documents section: named file uploads to R2, list/preview/download | [orders-projects](tab-orders-projects.md) | `smark_project_documents` (new) | — | 🟢 |
| R2-17 | "1 AI model can have all the context with encrypted names of client/items/product so company info is not leaked to AI but it still has context; not project description and stuff" | Pseudonymization layer for AI calls: alias map (CLIENT-A, ITEM-27) applied server-side before any Claude call carrying business context; descriptions/notes never sent; sourcing agents still use real MPN/LCSC (public catalog ids) | [agent-run](tab-agent-run.md), [ai-memory](tab-ai-memory.md) | `smark_ai_aliases` (new) | Purpose/scope of the "all-context" model → **Q-08** | 🟡 |
| R2-18 | "Owner should be able to assign engineers for the project and stuff" | Confirms R2-04: assignment = owner-only action ("engineers" = employee users) | [orders-projects](tab-orders-projects.md) | — | **Merged → R2-04** (assignment already planned; now pinned owner-only) | ⚪ |
| R2-19 | "Along with upload a BOM, create a BOM in-app with all required fields or add field like sheets; it should remember the structure for next BOMs" | In-app BOM builder: standard columns + add-custom-column grid; structure saved as remembered template, prefilled next time | [orders-projects](tab-orders-projects.md) | `smark_bom_templates` (new), `smark_bom_lines.extra jsonb` | Feeds same reconcile/pipeline as uploads (R2-03) | 🟢 |
| R2-20 | "New expenses tab, owner only: add expenses, income and stuff, more fields we will discuss later; show in daily report as well" | Owner-only Expenses tab: expense + income entries (fields draft until Q-09); today's entries appear in Daily Reports (owner eyes only) | [expenses](tab-expenses.md) (new), [daily-reports](tab-daily-reports.md) | `smark_expenses` (new, draft) | Fields + payments overlap → **Q-09**; tension with accountant role → noted in Q-01 | 🟡 |
| R2-21 | "Expense dashboard to track the company monthly/yearly and everything in good charts and stuff" | Charts view inside Expenses tab: monthly/yearly income vs expense, category breakdowns, trends | [expenses](tab-expenses.md) | rollup views over `smark_expenses` | Depends on R2-20 fields (Q-09) | 🟡 |
| R2-22 | "Mobile: add More as the 5th bottom button instead of AI Memory, show the other tabs there" | Mobile bottom bar: Dashboard · Inventory · Scan · Projects · **More** (sheet listing every remaining role-visible tab) | [login-shell](tab-login-shell.md) | — | Resolves the mobile-slot part of Q-01 (supersedes role-aware 5th-slot proposal) | 🟢 |
| R2-23 | "Receive stock: make it simple to use; in Add-a-part → New part add a custom-field option and save that custom field for later" | Simplify Receive UX (flatten nested toggles); part form gains "+ custom field" with remembered field definitions for future parts | [receive](tab-receive.md) | `smark_part_field_templates` (new); values → `parts.attributes` | Mirrors R2-19's remember-structure pattern | 🟢 |
| R2-24 | "In the inventory add the field for V separately" | Voltage becomes its own typed field: part form input, inventory column + facet (split out of the combined value string like `0.1µF/50V`) | [inventory](tab-inventory.md), [receive](tab-receive.md) | `smark_parts.voltage` promoted column | Import mapping must split value/voltage; confirm V = voltage | 🟢 |
| R2-25 | "Build all the other fields and stuff that are currently show-demo when clicked" | Blanket requirement: every stubbed/demo interaction in the prototype becomes real (full stub inventory listed in CROSS-FEATURE) | all tabs | — | Count/audit flow needs a mini-spec → **Q-10** | 🟢 |
| R2-26 | "Change the name for Bulk pick to Bulk takeout" | Rename everywhere (nav, screen title, buttons); internal `bulk_pick` reason key stays, display label changes | [bulk-pick](tab-bulk-pick.md), [login-shell](tab-login-shell.md) | — (display only) | — | 🟢 |
| R2-27 | "BOM for 1 unit but we require 10 of those built — ordering value changes 10× — add this extra field in the ordering workspace" | **Build quantity** field per BOM (default 1): all needs = line qty × build qty — reconcile split, skip-buy checks, agent min-qty, cart demand, cost estimate all multiply | [ordering-workspace](tab-ordering-workspace.md), [orders-projects](tab-orders-projects.md), [on-order→cart](tab-on-order.md), [bulk-pick](tab-bulk-pick.md) | `smark_boms.build_qty` | Interacts with R2-10 demand view (multiplied) | 🟢 |
| R2-28 | "Settings: add multiple accounts for expense tracking; ability to add employees; add rules; add keys for websites to order from" | Settings gains: expense **accounts** manager (cash/bank/UPI, owner-only); addable **distributors with keys**; employees + rules = already covered (R2-01 users card, baseline rules card) — confirmed | [settings](tab-settings.md), [expenses](tab-expenses.md) | `smark_expense_accounts` (new), `smark_expenses.account_id`, distributors addable | Employees→R2-01 ⚪-merge; rules→baseline; accounts tie into Q-09 | 🟢 |
| R2-29 | "Everything auto-tested on build, all passing, expected behaviour, no manual tests, no one thing breaking another, tests all the way to expected output" | Full automated quality gate: unit + RLS-matrix + API + worker + Playwright E2E (desktop/mobile) on every build; deploy blocked on red; every R2 change + invariant mapped to tests | [TESTING.md](TESTING.md) (new plan file) | test fixtures = canonical seed | Q-01 matrix becomes executable RLS spec; client's 500/400/200→100 example = permanent test | 🟢 |
| R2-30 | (Q-04 answer + estimate sheet) "Estimated timeline and planning — attached; make it so they can build this here per project, and according to that the project will proceed" | **Phase timeline builder** per project, modeled on their real sheet: phase rows (name, start/end, duration text, tasks/notes, parallel/buffer rows), current-phase tracking, shown on Overview + client share link | [orders-projects](tab-orders-projects.md), [client-portal](tab-client-portal.md) | `smark_project_phases` (new), `smark_projects.share_token` | **Supersedes R2-05's** two-date minimal (R2-05 → ⚪); drives Q-07 completion metrics | 🟢 |
| R2-31 | (I-01 approved) Duplicate-part guard | On new-part save: warn if MPN or value+package+voltage already matches an existing part ("Looks like SMK-000101 — top up instead?") with one-tap switch to top-up | [receive](tab-receive.md) | — (reuses reconcile matcher) | Merge tool = parked future (I-09) | 🟢 |
| R2-32 | (I-02 approved, "give a warning") Project archive/close | Explicit archive action with a **warning dialog** (releases cart demand, freezes activity, hides from active lists; reversible unarchive) | [orders-projects](tab-orders-projects.md), [on-order→cart](tab-on-order.md) | `smark_projects.archived_at` | Supplies the demand-release half of **Q-05** (rest still open) | 🟢 |
| R2-33 | (I-04 approved) Exports everywhere | CSV/xlsx export buttons on Inventory, Movements (daily report), Expenses, Attendance | [inventory](tab-inventory.md), [daily-reports](tab-daily-reports.md), [expenses](tab-expenses.md) | — (server-side generation) | — | 🟢 |
| R2-34 | (I-05 approved) Global search | Top-bar field + Ctrl-K palette searching parts, projects, BOMs, PO numbers — extends the existing scan-or-type field | [login-shell](tab-login-shell.md) | search indexes (pg_trgm/FTS) | — | 🟢 |
| R2-35 | (I-06 approved) Batch label print queue | Labels queue up (receive/onboarding) → print as full sheets in one go instead of per-part dialogs | [receive](tab-receive.md) | `smark_qr_labels.print_status` | Critical for the 2000-part onboarding | 🟢 |
| R2-36 | (I-07 approved) In-app notifications | Bell + badge: arrival marked, task assigned, suggested rule awaiting approval, low stock, run finished; per-role routing | [login-shell](tab-login-shell.md) | `smark_notifications` (new) | WhatsApp channel = future | 🟢 |
| R2-37 | (I-08 approved) AI spend meter | ₹ per run + monthly AI-spend series (from `agent_runs.actual_cost`) surfaced in Expenses charts (owner/accountant) | [expenses](tab-expenses.md) | — (reads agent_runs) | — | 🟢 |
| R2-38 | (I-10 approved) Client portal | Client-facing UI per project: phase timeline, progress, updates/reports — "good UI for the client"; access = tokenized link (Q-04) now, `client` role login = later upgrade seam | [client-portal](tab-client-portal.md) (new) | reads phases/status; `share_token` | Consolidates with R2-30 share link; portal comments land as `change` activities | 🟢 |

### Round-3 status flips (client's Q&A answers, 2026-07-02)

- **R2-01, R2-02 → 🟢** — Q-01 matrix agreed (accountant gets **Expenses write**), Q-02 PIN killed.
- **R2-02/04/07 → 🟢** — Q-03 closed: attendance = self clock-in/out; **hours = manual entry** per
  project per day (no derived model).
- **R2-05 → ⚪** superseded by R2-30 (phase timeline builder).
- **R2-15, R2-20, R2-21 → 🟢** — Q-09 approved (fields + accounts + PO→draft-expense + payments =
  income entries with project link).
- **R2-17 → 🟢** — Q-08: alias layer v1; chat assistant = Phase 4.
- **R2-12 amended by Q-06:** PO number = the **distributor website's order number** → checkout
  groups cart lines **by distributor**, one order row + order-number per group (delivery matching).
- **R2-10 stays 🟡** — Q-05 unanswered (client skipped); archive-release half settled by R2-32.
- **R2-14 stays 🟡** — Q-07 re-scoped: completion/performance to be derived from R2-30 phases;
  discuss next.
- **Declined:** I-03 low-stock auto-cart (client: "not that important"). **Parked future:** I-09
  merge-parts tool.

### Round-4 (final closures, 2026-07-02)

- **R2-10 → 🟢** — Q-05 default approved (register at reconcile; release on takeout/arrival/archive;
  dismissal-resurrect rule).
- **R2-14 → 🟢** — Q-07 approved (duration-weighted phase completion, active-phase on-track, buffer
  absorbs delay, done = last phase + owner confirm, no slider).
- **Board fully green: 36 🟢 · 2 ⚪ (superseded) · 0 open questions. FEATURES.md v2 regenerated
  from this folder — that file is now the build truth.**

## Notes on numbering

- Review-1 changes are already in the prototype and are **baseline** — they are not re-logged here.
  (The prototype source still carries comments like "change 17" / "change 20" from round 1.)
- If the client later amends one of their own R2 changes, do not edit the row's meaning — add a new
  row and mark the old one ⚪ superseded, pointing at the new ID. History stays auditable.
- A change that is purely a question / undecided goes straight to `PENDING-DECISIONS.md` and gets a
  row here with status 🔵 so the count still reaches ~50.
