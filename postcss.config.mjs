// Tailwind CSS v4 — chosen because the locked design system (theme.css /
// DESIGN.md "Tailwind v4" block) is authored in v4 `@theme` syntax and
// tailwindcss@4 + @tailwindcss/postcss install clean with bun.
const config = {
  plugins: ["@tailwindcss/postcss"],
};

export default config;
