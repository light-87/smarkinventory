/**
 * The latest SmarkStock Desktop version the server expects clients to run.
 * Bump this whenever a new installer is published so older installs show the
 * "update available" banner (desktop App.tsx checks it against its own version).
 */
export const LATEST_DESKTOP_VERSION = "0.6.0";
